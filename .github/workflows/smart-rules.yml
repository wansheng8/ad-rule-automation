name: 智能广告规则自动更新
on:
  schedule:
    - cron: '0 2 * * *'  # 每天UTC时间2点（北京时间10点）自动运行
  push:
    paths:
      - 'config/**'
      - 'scripts/**'
  workflow_dispatch:  # 允许手动触发

jobs:
  update-rules:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: 设置Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: 运行规则处理脚本
        run: python scripts/smart_rule_processor.py
        
      - name: 验证生成的文件
        run: |
          echo "=== 验证生成的文件 ==="
          
          # 检查dist目录
          if [ -d "dist" ]; then
            echo "📁 dist目录文件列表:"
            ls -la dist/
            echo ""
            
            # 检查 Adblock.txt
            if [ -f "dist/Adblock.txt" ]; then
              echo "✅ Adblock文件生成成功"
              echo "   文件大小: $(wc -c < dist/Adblock.txt | numfmt --to=iec)字节"
              echo "   文件行数: $(wc -l < dist/Adblock.txt)行"
              echo "   最后更新: $(head -5 dist/Adblock.txt | grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}' | head -1 || echo '未找到')"
            else
              echo "❌ Adblock.txt 文件未生成"
            fi
            
            # 检查 hosts.txt
            if [ -f "dist/hosts.txt" ]; then
              echo ""
              echo "✅ Hosts文件生成成功"
              echo "   文件大小: $(wc -c < dist/hosts.txt | numfmt --to=iec)字节"
              echo "   文件行数: $(wc -l < dist/hosts.txt)行"
              echo "   0.0.0.0格式: $(grep -c '^0\.0\.0\.0' dist/hosts.txt 2>/dev/null || echo '0')个"
              echo "   127.0.0.1格式: $(grep -c '^127\.0\.0\.1' dist/hosts.txt 2>/dev/null || echo '0')个"
            else
              echo "❌ hosts.txt 文件未生成"
            fi
            
            # 检查 Domains.txt
            if [ -f "dist/Domains.txt" ]; then
              echo ""
              echo "✅ Domains文件生成成功"
              echo "   文件大小: $(wc -c < dist/Domains.txt | numfmt --to=iec)字节"
              echo "   文件行数: $(wc -l < dist/Domains.txt)行"
              echo "   域名数量: $(grep -c '^[^#]' dist/Domains.txt 2>/dev/null || echo '0')个"
            else
              echo "❌ Domains.txt 文件未生成"
            fi
          else
            echo "❌ dist目录未生成"
          fi
          
          echo ""
          echo "=== 检查stats目录 ==="
          if [ -d "stats" ]; then
            echo "📁 stats目录文件列表:"
            ls -la stats/
            echo ""
            
            STATS_COUNT=$(find stats/ -type f -name "*.json" -o -name "*.md" 2>/dev/null | wc -l)
            if [ "$STATS_COUNT" -gt 0 ]; then
              echo "✅ stats目录有 $STATS_COUNT 个报告文件"
              # 显示最新报告的时间
              LATEST_STATS=$(find stats/ -type f -name "*.json" -printf "%T@ %p\n" 2>/dev/null | sort -n | tail -1 | cut -d' ' -f2-)
              if [ -n "$LATEST_STATS" ]; then
                echo "   最新报告: $(basename "$LATEST_STATS")"
                echo "   报告大小: $(wc -c < "$LATEST_STATS" | numfmt --to=iec)字节"
              fi
            else
              echo "⚠️  stats目录为空，脚本可能未生成报告"
            fi
          else
            echo "ℹ️  stats目录未生成"
          fi
          
      - name: 提交更新
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # 检查是否有变更
          if git diff --quiet; then
            echo "没有变更需要提交"
            exit 0
          fi
          
          echo "=== Git变更状态 ==="
          git status --short
          
          echo "准备提交，时间: $(date '+%Y-%m-%d %H:%M:%S')"
          git add .
          git commit -m "🤖 自动更新广告规则 [$(date '+%Y-%m-%d %H:%M:%S')]"
          
      - name: 推送变更
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
